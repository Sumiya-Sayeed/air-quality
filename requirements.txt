numpy>=1.24
pandas>=2.0
matplotlib>=3.7
requests
scikit-learn>=1.3   # Studies 4 (GBM baseline) and 5 (RQ-C hybrid)
# optional — only for scripts/download_era5.py (ERA5 meteorology):
# cdsapi>=0.7.7
# xarray
# netCDF4
